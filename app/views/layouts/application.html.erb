<!DOCTYPE html>
<html>
<head>
<meta charset=utf-8 />
<title>A simple map</title>
<meta name='viewport' content='initial-scale=1,maximum-scale=1,user-scalable=no' />
<script src='https://api.tiles.mapbox.com/mapbox.js/v2.1.9/mapbox.js'></script>
<link href='https://api.tiles.mapbox.com/mapbox.js/v2.1.9/mapbox.css' rel='stylesheet' />

</head>
<body>
<div id='ist-map'></div>
<script>
L.mapbox.accessToken = 'pk.eyJ1IjoianVjaHJ1IiwiYSI6IjZhYzFkMThlODBjNDY1N2NkNGZiZTVlMjI4MTU2OTY1In0.N1IDr2cpaPyTtanV-K2wdw';
// var map = L.mapbox.map('map', 'juchru.63054a99')

var geocoder = L.mapbox.geocoder('mapbox.places'),
    map = L.mapbox.map('map', 'juchru.63054a99');

geocoder.query('Istanbul, Turkey', showMap);

function showMap(err, data) {
    // The geocoder can return an area, like a city, or a
    // point, like an address. Here we handle both cases,
    // by fitting the map bounds to an area or zooming to a point.
    if (data.lbounds) {
        map.fitBounds(data.lbounds);
    } else if (data.latlng) {
        map.setView([data.latlng[0], data.latlng[1]], 13);
    }
}



</script>
</body>
</html>
