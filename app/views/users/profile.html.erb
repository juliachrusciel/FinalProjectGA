  <body id="profile-page">
    <%= render :partial => "nav" %>

    <h1>Merhaba, <%= @name %>!</h1>
    <p id="tag_line_1"> Update your username, email or password.</p>

    <form action="/profile" method="post">
      <div>
         <label for="username">Username:</label>
         <input type="text" id="username" />
     </div>
     <div>
       <label for="password">Password:</label>
       <input type="text" id="password" />
     </div>
     <div>
       <label for="mail">Email:</label>
       <input type="text" id="mail" />
     </div>
     <div class="button">
       <button type="submit">Update</button>
     </div>
    </form>



    <p id="tag_line_2">"To change your username, email or password, please reenter your information and change"</p>
    <footer></footer>

      <!-- <script type="text/javascript">

        $(document).ready(function(){
          $('#current_user').hide();

          $('#edit-button').prop("disabled", "true");


          // Line to renable user button
          $('#user_password').on('focusout', function() {

            $(':submit').prop("disabled", "")

          });



          $('#tag_line_1').on("click", function(evt){
            evt.preventDefault();
            $('#current_user').slideToggle("slow");

          });

          $('')

          // line to  disable user button until password is filled out

          $('input[name="user[password]"]').on('keyup', function(evt){
            evt.preventDefault();
            var emailField = $(this);
            if (emailField.val().length >= 6){
              emailField.css("background-color", "rgba(89,193,114,.5)");
            } else {
              emailField.css("background-color", "rgba(255,1,1,.5)");
            }
          });

        })

      </script> -->

  </body>
